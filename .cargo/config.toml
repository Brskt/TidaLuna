[alias]
xtask = "run --package xtask --"

# The Windows delay-load flags moved to build.rs: as a target-wide rustflag they also
# reached xtask and updater, which import nothing from libcef.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-Wl,-rpath,$ORIGIN/bin/cef"]
